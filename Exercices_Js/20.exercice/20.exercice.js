

const tache= prompt('choisi une tache "new" "list" "delete" ou "quit" ')
list=[]

if (tache == "new") { 
      list[list]=prompt("ajouter une action a la list"); 
    
  console.log(list)  
} 
if (tache == "list") { console.log(list)
    
}
